# Оптимизация изображений thumbnail

## Проблема

Исходная реализация загружала изображения в размере **1200x800px**, что избыточно для карточек артефактов:

- **Реальный размер карточки**: 300-400px на десктопе
- **Mobile**: ~375px ширина экрана
- **Retina displays**: даже с 2x DPR достаточно 600-800px
- **Лишний трафик**: изображение 1200px весит в 2-3 раза больше необходимого
- **Квоты Cloudinary**: бесплатный план ограничен 25 GB/месяц

## Решение

### 1. Уменьшен размер загрузки

**До**: 1200x800px
**После**: 600x400px

```typescript
// src/config/cloudinary.ts
export const IMAGE_CONSTRAINTS = {
  maxWidth: 600,   // Уменьшено с 1200
  maxHeight: 400,  // Уменьшено с 800
  quality: 85,
  format: 'jpg',
};
```

### 2. Динамическая оптимизация через Cloudinary Transformations

Вместо загрузки полного изображения используем Cloudinary transformations API:

```typescript
// src/utils/thumbnailOptimizer.ts
export function getOptimizedThumbnail(url: string): string {
  // Для Cloudinary URL применяем трансформации
  return getOptimizedUrl(publicId, {
    width: 600,
    height: 400,
    crop: 'fill',
    quality: 85,
  });
}
```

**Пример URL**:
```
// Оригинал
https://res.cloudinary.com/balaproza/image/upload/artifacts/abc123.jpg

// Оптимизированный (автоматически)
https://res.cloudinary.com/balaproza/image/upload/w_600,h_400,c_fill,q_85/artifacts/abc123.jpg
```

### 3. Lazy loading

Добавлен атрибут `loading="lazy"` для отложенной загрузки изображений:

```tsx
<img
  src={thumbnailUrl}
  alt={group.title}
  className="w-full h-full object-cover"
  loading="lazy"  // ← Загрузка только при скролле
/>
```

## Экономия

### Размер файлов (примерно)

| Размер | Вес (JPEG 85%) | Экономия |
|--------|----------------|----------|
| 1200x800px | ~150-250 KB | - |
| 600x400px | ~40-70 KB | **70-75%** |
| 300x200px | ~15-25 KB | **85-90%** |

### Bandwidth для 100 карточек

| Размер | Traffic | Разница |
|--------|---------|---------|
| 1200x800px | ~20 MB | - |
| 600x400px | **~5 MB** | **-15 MB** |

### Квоты Cloudinary (Free tier)

**До оптимизации**:
- 25 GB / 20 MB на 100 карточек = **1,250 полных загрузок/месяц**

**После оптимизации**:
- 25 GB / 5 MB на 100 карточек = **5,000 полных загрузок/месяц** 🚀

## Использование в коде

### ArtifactCard (автоматическая оптимизация)

```tsx
import { getOptimizedThumbnail } from '../../utils/thumbnailOptimizer';

const thumbnailUrl = group.thumbnail
  ? getOptimizedThumbnail(group.thumbnail)
  : null;

<img src={thumbnailUrl} alt={group.title} loading="lazy" />
```

### Для маленьких превью (если нужно)

```tsx
import { getSmallThumbnail } from '../../utils/thumbnailOptimizer';

const smallThumb = getSmallThumbnail(group.thumbnail);
// Размер: 300x200px, quality: 80%
```

### Для внешних URL (не Cloudinary)

Функция `getOptimizedThumbnail()` автоматически определяет:
- **Cloudinary URL** → применяются трансформации
- **Внешний URL** → возвращается без изменений

## Преимущества

✅ **Быстрая загрузка** — меньше времени ожидания для пользователей
✅ **Экономия трафика** — в 4 раза меньше bandwidth
✅ **Больше квоты** — в 4 раза больше загрузок в месяц
✅ **Лучший UX** — lazy loading, оптимальное качество
✅ **Retina-ready** — 600px достаточно для 2x DPR на карточках ~300px
✅ **Автоматично** — работает прозрачно для существующего кода

## Дополнительные улучшения (будущее)

### Responsive images (srcset)

Можно добавить адаптивные изображения для разных размеров экрана:

```tsx
<img
  src={getOptimizedThumbnail(url)}
  srcSet={`
    ${getSmallThumbnail(url)} 300w,
    ${getOptimizedThumbnail(url)} 600w,
    ${getOptimizedUrl(publicId, { width: 1200 })} 1200w
  `}
  sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 25vw"
/>
```

### WebP format

Cloudinary автоматически конвертирует в WebP для браузеров с поддержкой:

```typescript
export function getOptimizedUrl(publicId, options) {
  // Добавить f_auto для автоматического выбора формата
  transformations.push('f_auto');
  // Cloudinary выберет WebP для Chrome/Firefox, JPEG для Safari
}
```

### Blur placeholder

Можно добавить размытый placeholder для улучшения восприятия загрузки:

```typescript
export function getBlurPlaceholder(url: string): string {
  return getOptimizedUrl(publicId, {
    width: 40,
    height: 40,
    quality: 30,
    blur: 800,
  });
}
```
